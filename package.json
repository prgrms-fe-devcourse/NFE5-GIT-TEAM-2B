{
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "scripts": {
    "api": "json-server --watch db.json --port 3001",
    "dev": "live-server --port=8080 --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
